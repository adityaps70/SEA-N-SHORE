'use client';
import { useState } from 'react';
import { Card } from '@/components/ui';
import { articles } from '@/lib/data/seed';
import { BellRing, BookOpenText, MessageCircle, ShieldAlert } from 'lucide-react';
export default function KnowledgePage(){const [saved,setSaved]=useState<string[]>([]);return <div className="space-y-4"><div><div className="sns-page-title">Maritime Knowledge Hub</div><p className="sns-page-copy mt-1">Regulatory context, safety learning and industry analysis with community discussion under every article.</p></div><div className="grid gap-3 sm:grid-cols-3"><Card className="p-4"><BellRing size={19} className="text-[#1167d8]"/><b className="mt-3 block text-lg text-[#173b59]">IMO & regulation</b><span className="text-xs text-[#728799]">What changed + why it matters</span></Card><Card className="p-4"><ShieldAlert size={19} className="text-[#1167d8]"/><b className="mt-3 block text-lg text-[#173b59]">Safety alerts</b><span className="text-xs text-[#728799]">Learn from incidents and near misses</span></Card><Card className="p-4"><BookOpenText size={19} className="text-[#1167d8]"/><b className="mt-3 block text-lg text-[#173b59]">Industry analysis</b><span className="text-xs text-[#728799]">Technical + commercial context</span></Card></div><div className="space-y-3">{articles.map(a=><Card className="p-5" key={a.id}><div className="flex flex-wrap items-center justify-between gap-3"><span className={`sns-pill ${a.type==='Safety Alert'?'sns-pill-green':'sns-pill-blue'}`}>{a.type}</span><span className="text-xs text-[#8295a4]">{a.read}</span></div><h2 className="mt-4 text-xl font-black leading-7 text-[#173b59]">{a.title}</h2><p className="mt-2 text-sm leading-6 text-[#60798c]">{a.summary}</p><div className="mt-4 flex items-center gap-3 border-t border-[#edf2f5] pt-4 text-xs"><span className="font-bold text-[#526d82]">{a.source}</span><span className="ml-auto flex items-center gap-1 text-[#728799]"><MessageCircle size={14}/>{a.discussions} discussions</span><button onClick={()=>setSaved(saved.includes(a.id)?saved.filter(x=>x!==a.id):[...saved,a.id])} className="font-black text-[#1167d8]">{saved.includes(a.id)?'Saved':'Save'}</button></div></Card>)}</div></div>}
